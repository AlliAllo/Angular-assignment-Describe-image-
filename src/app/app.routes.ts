import { Routes } from '@angular/router';
import { ImageLoaderComponent } from './components/imageLoader/image-loader.component';

export const routes: Routes = [
  { path: '', component: ImageLoaderComponent }
];
